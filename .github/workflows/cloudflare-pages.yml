name: Cloudflare deployment (disabled)

# This workflow was neutralized to disable Cloudflare Pages deploys.
# Repo is being consolidated to Vercel-only hosting; the original
# Cloudflare workflow was kept for history but does nothing.
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Cloudflare Pages deploy removed; use Vercel for deployments."
