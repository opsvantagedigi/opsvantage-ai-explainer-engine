name: Netlify deployment (disabled)

# Netlify workflow neutralized — Netlify deploys removed in favor of Vercel.
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Netlify deploy removed; use Vercel for deployments."
