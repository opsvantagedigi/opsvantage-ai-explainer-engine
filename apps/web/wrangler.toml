## Wrangler config removed — Cloudflare Pages integration disabled.
# The original file pointed Pages output to ".vercel/output/static".
# Kept for history but Cloudflare deploys are disabled; Vercel is the
# target platform.

